library(ggplot2)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_lines() +
geom_points() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_lines() +
geom_points() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_line() +
geom_point() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
# start with saturated model to test for additivity and iterate
# until you settle on a final model for inference. Be sure to assess assumptions!
biggie_forward <- step(lm(tot_time ~ age_group, data = df_big),
scope = tot_time ~ age_group * Gender,
direction = "forward")
summary(biggie_forward)
# conduct follow up tests and/or find CIs for contrasts to address secondary questions
# create plots to summarize findings
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
library(GGally)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_line() +
geom_point() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
ggpairs(data=df_big, columns = 1:4, aes(color = Species))
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
library(GGally)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_line() +
geom_point() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
ggpairs(data=df_big, columns = 1:4)
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
library(GGally)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_line() +
geom_point() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
ggpairs(data=summ_table, c("tot_time", "age_group", "Gender"))
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
library(GGally)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_line() +
geom_point() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
ggpairs(data=df_big, c("tot_time", "age_group", "Gender"))
# start with saturated model to test for additivity and iterate
# until you settle on a final model for inference. Be sure to assess assumptions!
biggie_forward <- step(lm(tot_time ~ age_group, data = df_big),
scope = tot_time ~ age_group * Gender,
direction = "forward")
summary(biggie_forward)
# conduct follow up tests and/or find CIs for contrasts to address secondary questions
# create plots to summarize findings
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
library(GGally)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_line() +
geom_point() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
# start with saturated model to test for additivity and iterate
# until you settle on a final model for inference. Be sure to assess assumptions!
biggie_forward <- step(lm(tot_time ~ age_group, data = df_big),
scope = tot_time ~ age_group * Gender,
direction = "forward")
summary(biggie_forward)
# conduct follow up tests and/or find CIs for contrasts to address secondary questions
# create plots to summarize findings
View(df_big)
View(df_big)
knitr::opts_chunk$set(echo = TRUE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
library(GGally)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_line() +
geom_point() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
# start with saturated model to test for additivity and iterate
# until you settle on a final model for inference. Be sure to assess assumptions!
biggie_int <- lm(tot_time ~ age_group * Gender, data = df_big)
biggie_add <- lm(tot_time ~ age_group + Gender, data = df_big)
anova(biggie_add, biggie_int)
# conduct follow up tests and/or find CIs for contrasts to address secondary questions
# create plots to summarize findings
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
library(GGally)
library(emmeans)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_line() +
geom_point() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
# start with saturated model to test for additivity and iterate
# until you settle on a final model for inference. Be sure to assess assumptions!
biggie_int <- lm(tot_time ~ age_group * Gender, data = df_big)
biggie_add <- lm(tot_time ~ age_group + Gender, data = df_big)
anova(biggie_add, biggie_int)
# conduct follow up tests and/or find CIs for contrasts to address secondary questions
emm_biggie <- emmeans(biggie_add, specs = ~age_group)
emm_biggie
# create plots to summarize findings
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
library(GGally)
library(emmeans)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_line() +
geom_point() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age")
# start with saturated model to test for additivity and iterate
# until you settle on a final model for inference. Be sure to assess assumptions!
biggie_int <- lm(tot_time ~ age_group * Gender, data = df_big)
biggie_add <- lm(tot_time ~ age_group + Gender, data = df_big)
anova(biggie_add, biggie_int)
par(mfrow = c(2,2))
plot(biggie_add, pch = 18)
# conduct follow up tests and/or find CIs for contrasts to address secondary questions
emm_biggie <- emmeans(biggie_add, specs = ~age_group)
emm_biggie
# create plots to summarize findings
# load packages
library(pander)
library(tidyverse)
library(ggplot2)
library(GGally)
library(emmeans)
# load data (note you may need to change the path). The path below indicates
# the dataset is in a data folder that exists in the folder where this .Rmd is
# saved.
df_biggie <- read_csv("df_biggie30clean.csv")
glimpse(df_biggie)
# there are some racers who dropped (i.e., did not finish)
# let's remove them from the analysis
df_big <- df_biggie |>
filter(Place != "Drop")
# compare the number of rows in df_biggie and df_big to see how many racers
# dropped
glimpse(df_big)
# lets look at some summary statistics
summ_table <- df_big |>
group_by(age_group, Gender) |>
summarise(
avg_time = mean(tot_time),
sd_time = mean(tot_time),
n_group = n()
)
# think about what the code above did and what it tells you about
summ_table
# make fancy table output that goes directly in a markdown file
pander::pander(summ_table, caption = "YOUR CAPTION HERE")
# make a raw data and summary plots
summ_table |>
ggplot(aes(x = age_group, y = avg_time, group = Gender, colour = Gender)) +
geom_line() +
geom_point() +
labs(x = "Age Group",
y = "Average Time (Min)",
title = "Big Sky Biggie Average Time by Age Group")
# start with saturated model to test for additivity and iterate
# until you settle on a final model for inference. Be sure to assess assumptions!
biggie_int <- lm(tot_time ~ age_group * Gender, data = df_big)
biggie_add <- lm(tot_time ~ age_group + Gender, data = df_big)
anova(biggie_add, biggie_int)
par(mfrow = c(2,2))
plot(biggie_add, pch = 18)
# conduct follow up tests and/or find CIs for contrasts to address secondary questions
emm_biggie <- emmeans(biggie_add, specs = ~age_group)
emm_biggie
# create plots to summarize findings
