# to factors.
# remove rows with incomplete cases --- note this assumes the values were
# missing at random and there is no association between "missingness" and
# the outcome of the response variable.
sleep_df <- mammalsleep %>%
mutate(exposure_level = factor(ifelse(exposure == 1, "Low", "High")),
predation_c = factor(predation), # top two vars for Part II of HW
PS = dream, # rename to match paper
log_PS = log(dream + 0.05), # add 0.05 to all PS sleep times
# to include mammal with 0 PS in analysis on log_PS scale
SWS = nondream, # rename to match paper
lgest = log(gestation), # log transformations from paper
lbrain = log(brain),
lbody = log(body)) %>%
drop_na() # remove mammals with NAs in any of the rows of the data frame
# create a data frame for part I analyses
df_sleep <- sleep_df %>%
dplyr::select(body, lbody, brain, lbrain, gestation, lgest, lifespan,
predation, exposure, danger, sleep, PS, log_PS, SWS)
# I'm assuming the prompt wants brain weight instead of brain size
df_sleep |> select(gestation, body, brain, SWS) |>
GGally::ggpairs()
sleep.step <- step(lm(SWS ~ 1,
data = df_sleep),
scope = SWS ~ lbody + lbrain +
lgest + lifespan + predation +
exposure + danger,
direction = "forward")
summary(sleep.step)
par(mfrow = c(2,2))
plot(sleep.step, pch = 18)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
# I'm assuming the prompt wants brain weight instead of brain size
df_sleep |> select(gestation, body, brain, SWS) |>
GGally::ggpairs()
sleep.step <- step(lm(SWS ~ 1,
data = df_sleep),
scope = SWS ~ lbody + lbrain +
lgest + lifespan + predation +
exposure + danger,
direction = "forward")
summary(sleep.step)
par(mfrow = c(2,2))
plot(sleep.step, pch = 18)
sleep.step <- step(lm(SWS ~ 1,
data = df_sleep),
scope = SWS ~ lbody + lbrain +
lgest + lifespan + predation +
exposure + danger,
direction = "forward")
summary(sleep.step)
par(mfrow = c(2,2))
plot(sleep.step, pch = 18)
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
data <- data(mammalsleep)
# make some transformations made by authors and reclass string variables
# to factors.
# remove rows with incomplete cases --- note this assumes the values were
# missing at random and there is no association between "missingness" and
# the outcome of the response variable.
sleep_df <- data %>%
mutate(exposure_level = factor(ifelse(exposure == 1, "Low", "High")),
predation_c = factor(predation), # top two vars for Part II of HW
PS = dream, # rename to match paper
log_PS = log(dream + 0.05), # add 0.05 to all PS sleep times
# to include mammal with 0 PS in analysis on log_PS scale
SWS = nondream, # rename to match paper
lgest = log(gestation), # log transformations from paper
lbrain = log(brain),
lbody = log(body)) %>%
drop_na() # remove mammals with NAs in any of the rows of the data frame
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
data <- mammalsleep
# make some transformations made by authors and reclass string variables
# to factors.
# remove rows with incomplete cases --- note this assumes the values were
# missing at random and there is no association between "missingness" and
# the outcome of the response variable.
sleep_df <- data %>%
mutate(exposure_level = factor(ifelse(exposure == 1, "Low", "High")),
predation_c = factor(predation), # top two vars for Part II of HW
PS = dream, # rename to match paper
log_PS = log(dream + 0.05), # add 0.05 to all PS sleep times
# to include mammal with 0 PS in analysis on log_PS scale
SWS = nondream, # rename to match paper
lgest = log(gestation), # log transformations from paper
lbrain = log(brain),
lbody = log(body)) %>%
drop_na() # remove mammals with NAs in any of the rows of the data frame
# create a data frame for part I analyses
df_sleep <- sleep_df %>%
dplyr::select(body, lbody, brain, lbrain, gestation, lgest, lifespan,
predation, exposure, danger, sleep, PS, log_PS, SWS)
# I'm assuming the prompt wants brain weight instead of brain size
df_sleep |> select(gestation, body, brain, SWS) |>
GGally::ggpairs()
sleep.step <- step(lm(SWS ~ 1,
data = df_sleep),
scope = SWS ~ lbody + lbrain +
lgest + lifespan + predation +
exposure + danger,
direction = "forward")
summary(sleep.step)
remove.packages("GGally")
install.packages("GGally")
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
data <- mammalsleep
remove.packages("GGally")
# load packages
library(faraway)
library(tidyverse)
library(GGally)
install.packages("GGally")
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
data <- mammalsleep
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
data(mammalsleep)
? mammalsleep
# make some transformations made by authors and reclass string variables
# to factors.
# remove rows with incomplete cases --- note this assumes the values were
# missing at random and there is no association between "missingness" and
# the outcome of the response variable.
sleep_df <- mammalsleep |>
mutate(exposure_level = factor(ifelse(exposure == 1, "Low", "High")),
predation_c = factor(predation), # top two vars for Part II of HW
PS = dream, # rename to match paper
log_PS = log(dream + 0.05), # add 0.05 to all PS sleep times
# to include mammal with 0 PS in analysis on log_PS scale
SWS = nondream, # rename to match paper
lgest = log(gestation), # log transformations from paper
lbrain = log(brain),
lbody = log(body)) %>%
drop_na() # remove mammals with NAs in any of the rows of the data frame
# create a data frame for part I analyses
df_sleep <- sleep_df %>%
dplyr::select(body, lbody, brain, lbrain, gestation, lgest, lifespan,
predation, exposure, danger, sleep, PS, log_PS, SWS)
# I'm assuming the prompt wants brain weight instead of brain size
df_sleep |> select(gestation, body, brain, SWS) |>
GGally::ggpairs()
sleep.step <- step(lm(SWS ~ 1,
data = df_sleep),
scope = SWS ~ lbody + lbrain +
lgest + lifespan + predation +
exposure + danger,
direction = "forward")
summary(sleep.step)
par(mfrow = c(2,2))
plot(sleep.step, pch = 18)
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
data(mammalsleep)
? mammalsleep
# make some transformations made by authors and reclass string variables
# to factors.
# remove rows with incomplete cases --- note this assumes the values were
# missing at random and there is no association between "missingness" and
# the outcome of the response variable.
sleep_df <- mammalsleep |>
mutate(exposure_level = factor(ifelse(exposure == 1, "Low", "High")),
predation_c = factor(predation), # top two vars for Part II of HW
PS = dream, # rename to match paper
log_PS = log(dream + 0.05), # add 0.05 to all PS sleep times
# to include mammal with 0 PS in analysis on log_PS scale
SWS = nondream, # rename to match paper
lgest = log(gestation), # log transformations from paper
lbrain = log(brain),
lbody = log(body)) %>%
drop_na() # remove mammals with NAs in any of the rows of the data frame
# create a data frame for part I analyses
df_sleep <- sleep_df %>%
dplyr::select(body, lbody, brain, lbrain, gestation, lgest, lifespan,
predation, exposure, danger, sleep, PS, log_PS, SWS)
# I'm assuming the prompt wants brain weight instead of brain size
df_sleep |> select(gestation, body, brain, SWS) |>
GGally::ggpairs()
sleep.step <- step(lm(SWS ~ 1,
data = df_sleep),
scope = SWS ~ lbody + lbrain +
lgest + lifespan + predation +
exposure + danger,
direction = "forward")
summary(sleep.step)
par(mfrow = c(2,2))
plot(sleep.step, pch = 18)
unlink("statX12-hw2_partI_sp26complete_cache", recursive = TRUE)
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
data(mammalsleep)
? mammalsleep
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE, eval=T)
options(show.signif.stars = FALSE)
data(mammalsleep)
? mammalsleep
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE, eval=T)
options(show.signif.stars = FALSE)
data(mammalsleep)
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE, eval=T)
options(show.signif.stars = FALSE)
data(mammalsleep)
write.csv(mammalsleep, "mammalsleep.csv", row.names = FALSE)
# Later, read it back
df <- read.csv("mammalsleep.csv")
library(readr)
mammalsleep <- read_csv("mammalsleep.csv")
View(mammalsleep)
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
# load packages
#library(faraway)
#library(tidyverse)
#library(GGally)
#data(mammalsleep)
#? mammalsleep
# make some transformations made by authors and reclass string variables
# to factors.
# remove rows with incomplete cases --- note this assumes the values were
# missing at random and there is no association between "missingness" and
# the outcome of the response variable.
sleep_df <- mammalsleep %>%
mutate(exposure_level = factor(ifelse(exposure == 1, "Low", "High")),
predation_c = factor(predation), # top two vars for Part II of HW
PS = dream, # rename to match paper
log_PS = log(dream + 0.05), # add 0.05 to all PS sleep times
# to include mammal with 0 PS in analysis on log_PS scale
SWS = nondream, # rename to match paper
lgest = log(gestation), # log transformations from paper
lbrain = log(brain),
lbody = log(body)) %>%
drop_na() # remove mammals with NAs in any of the rows of the data frame
# create a data frame for part I analyses
df_sleep <- sleep_df %>%
dplyr::select(body, lbody, brain, lbrain, gestation, lgest, lifespan,
predation, exposure, danger, sleep, PS, log_PS, SWS)
# I'm assuming the prompt wants brain weight instead of brain size
df_sleep %>% select(gestation, body, brain, SWS) |>
GGally::ggpairs()
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE, eval=T)
options(show.signif.stars = FALSE)
#data(mammalsleep)
#write.csv(mammalsleep, "mammalsleep.csv", row.names = FALSE)
# Later, read it back
df <- read.csv("mammalsleep.csv")
# make some transformations made by authors and reclass string variables
# to factors.
# remove rows with incomplete cases --- note this assumes the values were
# missing at random and there is no association between "missingness" and
# the outcome of the response variable.
sleep_df <- df |>
mutate(exposure_level = factor(ifelse(exposure == 1, "Low", "High")),
predation_c = factor(predation), # top two vars for Part II of HW
PS = dream, # rename to match paper
log_PS = log(dream + 0.05), # add 0.05 to all PS sleep times
# to include mammal with 0 PS in analysis on log_PS scale
SWS = nondream, # rename to match paper
lgest = log(gestation), # log transformations from paper
lbrain = log(brain),
lbody = log(body)) %>%
drop_na() # remove mammals with NAs in any of the rows of the data frame
# create a data frame for part I analyses
df_sleep <- sleep_df %>%
dplyr::select(body, lbody, brain, lbrain, gestation, lgest, lifespan,
predation, exposure, danger, sleep, PS, log_PS, SWS)
# I'm assuming the prompt wants brain weight instead of brain size
df_sleep |> select(gestation, body, brain, SWS) |>
GGally::ggpairs()
sleep.step <- step(lm(SWS ~ 1,
data = df_sleep),
scope = SWS ~ lbody + lbrain +
lgest + lifespan + predation +
exposure + danger,
direction = "forward")
summary(sleep.step)
par(mfrow = c(2,2))
plot(sleep.step, pch = 18)
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE, eval=T)
options(show.signif.stars = FALSE)
#data(mammalsleep)
#write.csv(mammalsleep, "mammalsleep.csv", row.names = FALSE)
getwd()
# Later, read it back
df <- read.csv("mammalsleep.csv")
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE, eval=T)
options(show.signif.stars = FALSE)
#data(mammalsleep)
#write.csv(mammalsleep, "mammalsleep.csv", row.names = FALSE)
getwd()
# Later, read it back
df <- read.csv("mammalsleep.csv")
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE, eval=T)
options(show.signif.stars = FALSE)
#data(mammalsleep)
#write.csv(mammalsleep, "mammalsleep.csv", row.names = FALSE)
getwd()
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE, eval=T)
options(show.signif.stars = FALSE)
data(mammalsleep)
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE, eval=T)
options(show.signif.stars = FALSE)
data(mammalsleep)
# make some transformations made by authors and reclass string variables
# to factors.
# remove rows with incomplete cases --- note this assumes the values were
# missing at random and there is no association between "missingness" and
# the outcome of the response variable.
sleep_df <- df |>
mutate(exposure_level = factor(ifelse(exposure == 1, "Low", "High")),
predation_c = factor(predation), # top two vars for Part II of HW
PS = dream, # rename to match paper
log_PS = log(dream + 0.05), # add 0.05 to all PS sleep times
# to include mammal with 0 PS in analysis on log_PS scale
SWS = nondream, # rename to match paper
lgest = log(gestation), # log transformations from paper
lbrain = log(brain),
lbody = log(body)) %>%
drop_na() # remove mammals with NAs in any of the rows of the data frame
# create a data frame for part I analyses
df_sleep <- sleep_df %>%
dplyr::select(body, lbody, brain, lbrain, gestation, lgest, lifespan,
predation, exposure, danger, sleep, PS, log_PS, SWS)
# I'm assuming the prompt wants brain weight instead of brain size
df_sleep |> select(gestation, body, brain, SWS) |>
GGally::ggpairs()
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
data(mammalsleep)
# make some transformations made by authors and reclass string variables
# to factors.
# remove rows with incomplete cases --- note this assumes the values were
# missing at random and there is no association between "missingness" and
# the outcome of the response variable.
sleep_df <- df |>
mutate(exposure_level = factor(ifelse(exposure == 1, "Low", "High")),
predation_c = factor(predation), # top two vars for Part II of HW
PS = dream, # rename to match paper
log_PS = log(dream + 0.05), # add 0.05 to all PS sleep times
# to include mammal with 0 PS in analysis on log_PS scale
SWS = nondream, # rename to match paper
lgest = log(gestation), # log transformations from paper
lbrain = log(brain),
lbody = log(body)) %>%
drop_na() # remove mammals with NAs in any of the rows of the data frame
# create a data frame for part I analyses
df_sleep <- sleep_df %>%
dplyr::select(body, lbody, brain, lbrain, gestation, lgest, lifespan,
predation, exposure, danger, sleep, PS, log_PS, SWS)
# I'm assuming the prompt wants brain weight instead of brain size
df_sleep |> select(gestation, body, brain, SWS) |>
GGally::ggpairs()
# I'm assuming the prompt wants brain weight instead of brain size
df_sleep |> select(gestation, body, brain, SWS) |>
GGally::ggpairs()
# load packages
library(faraway)
library(tidyverse)
library(GGally)
library(dplyr)
knitr::opts_chunk$set(echo = FALSE,
warning = FALSE, message = FALSE)
options(show.signif.stars = FALSE)
data(mammalsleep)
# make some transformations made by authors and reclass string variables
# to factors.
# remove rows with incomplete cases --- note this assumes the values were
# missing at random and there is no association between "missingness" and
# the outcome of the response variable.
sleep_df <- mammalsleep |>
mutate(exposure_level = factor(ifelse(exposure == 1, "Low", "High")),
predation_c = factor(predation), # top two vars for Part II of HW
PS = dream, # rename to match paper
log_PS = log(dream + 0.05), # add 0.05 to all PS sleep times
# to include mammal with 0 PS in analysis on log_PS scale
SWS = nondream, # rename to match paper
lgest = log(gestation), # log transformations from paper
lbrain = log(brain),
lbody = log(body)) %>%
drop_na() # remove mammals with NAs in any of the rows of the data frame
# create a data frame for part I analyses
df_sleep <- sleep_df %>%
dplyr::select(body, lbody, brain, lbrain, gestation, lgest, lifespan,
predation, exposure, danger, sleep, PS, log_PS, SWS)
